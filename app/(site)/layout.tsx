import React from 'react';

export const metadata = {
  title: 'SceneHim by Cruizr',
  description: 'Selective realms, atmospheric maps, and fashion-editorial discovery.',
};

export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
